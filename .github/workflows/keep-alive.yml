name: Keep Render and Supabase Alive

on:
  workflow_dispatch:

  schedule:
    - cron: '3,13,23,33,43,53 * * * *'

jobs:
  keep-alive:
    runs-on: ubuntu-latest

    steps:
      - name: Call backend database health endpoint
        run: |
          curl --fail --silent --show-error --max-time 30 "${{ secrets.RENDER_HEALTH_DB_URL }}"
